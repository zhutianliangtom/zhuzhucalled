[x] 后端 /admin/version/publish 接口校验 versionCode 递增逻辑正确实现

[x] 后端 /admin/version/publish 接口校验版本号格式正确实现

[x] 后端 /version/latest 接口返回 forceUpdate 为布尔类型

[x] 后端 /version/latest 接口在无版本记录时返回 { data: null }

[x] App.vue checkUpdate() 网络请求失败时显示错误提示

[x] App.vue checkUpdate() 后端返回 null 时显示提示

[x] App.vue checkUpdate() forceUpdate 字段判断使用 === true

[x] App.vue checkUpdate() 已是最新版本时显示提示

[x] App.vue 移除 hasShownUpdateNotification 一次性跳过标记

[x] App.vue 强制更新时用户取消则调用 plus.runtime.quit() 退出 App

[x] App.vue 强制更新时设置 forceQuitHandled 标记防止本次启动重复弹窗

[x] App.vue 非强制更新时用户可以跳过，正常进入 App

[x] settings.vue checkUpdate() 网络请求失败时显示错误提示

[x] settings.vue checkUpdate() 后端返回 null 时显示提示

[x] settings.vue checkUpdate() forceUpdate 字段判断使用 === true

[x] settings.vue checkUpdate() 已是最新版本时显示提示
